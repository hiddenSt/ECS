#ifndef ECS__TYPES_HPP_
#define ECS__TYPES_HPP_

#include <cstddef>

namespace ecs {

using EntityId = std::size_t;
using ComponentTypeId = std::size_t;
using SystemTypeId = std::size_t;

}  // namespace ecs

#endif  // ECS__TYPES_HPP_
